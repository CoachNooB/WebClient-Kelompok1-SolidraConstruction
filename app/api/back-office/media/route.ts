import {headers} from "next/headers";import {NextResponse} from "next/server";import {auth} from "@/lib/auth";import {can,type StaffRole} from "@/lib/auth/permissions";import {prisma} from "@/lib/db";import {removePublicAsset,uploadPublicAsset} from "@/lib/storage/supabase";
export async function POST(request:Request){const session=await auth.api.getSession({headers:await headers()});if(!session||!can(session.user.role as StaffRole,"content:write"))return NextResponse.json({error:"Forbidden"},{status:403});const form=await request.formData();const file=form.get("file");if(!(file instanceof File))return NextResponse.json({error:"File required"},{status:422});let uploaded:{path:string;publicUrl:string}|undefined;try{uploaded=await uploadPublicAsset(file,"image");const media=await prisma.mediaAsset.create({data:{storagePath:uploaded.path,fileName:file.name,mimeType:file.type,size:file.size,altId:String(form.get("altId")??""),altEn:String(form.get("altEn")??""),ownerId:session.user.id},select:{id:true}});return NextResponse.json(media,{status:201})}catch(error){if(uploaded)await removePublicAsset(uploaded.path,"image").catch(()=>undefined);return NextResponse.json({error:error instanceof Error?error.message:"Upload failed"},{status:500})}}
