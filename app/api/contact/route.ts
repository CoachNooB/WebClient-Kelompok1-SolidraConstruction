import {NextResponse} from "next/server"; import {contactSchema} from "@/lib/validation/submissions";
export async function POST(request:Request){const form=await request.formData();const parsed=contactSchema.safeParse({name:form.get("name"),email:form.get("email"),phone:form.get("phone")??"",company:form.get("company")??"",subject:form.get("subject"),message:form.get("message"),consent:form.get("consent")==="true",website:form.get("website")??""});if(!parsed.success)return NextResponse.json({error:"Invalid submission",fields:parsed.error.flatten().fieldErrors},{status:422});return NextResponse.json({ok:true},{status:201})}
