import {notFound} from "next/navigation"; const sections=["pages","investor-documents","vacancies","applications","messages","media","navigation","footer","settings","users"];
export default async function Section({params}:{params:Promise<{section:string}>}){const {section}=await params;if(!sections.includes(section))notFound();const title=section.split("-").map(x=>x[0].toUpperCase()+x.slice(1)).join(" ");return <><p className="eyebrow">Content management</p><div className="mt-2 flex items-center justify-between"><h1 className="text-3xl font-black">{title}</h1><button className="btn btn-primary">Add new</button></div><div className="card mt-8"><h2 className="font-bold">{title} management</h2><p className="mt-2 text-slate-600">This structured management screen is ready for database-backed records after environment setup.</p></div></>}
