import type {MetadataRoute} from "next"; import {vacancies} from "@/lib/content"; export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL??"http://localhost:3000";const pages=["","/about","/investors","/contact","/careers"];return ["id","en"].flatMap(locale=>[...pages.map(path=>({url:`${base}/${locale}${path}`,lastModified:new Date()})),...vacancies.map(v=>({url:`${base}/${locale}/careers/${v.id}`,lastModified:new Date()}))])}
