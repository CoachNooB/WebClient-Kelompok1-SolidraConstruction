"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {Button} from "@/components/ui/button";import {ConfirmButton} from "@/components/back-office/confirm-button";import {ToastMessage} from "@/components/back-office/toast";
export function ResourceStatusForm({endpoint,status,statuses}:{endpoint:string;status:string;statuses:string[]}){const router=useRouter();const [message,setMessage]=useState<{text:string;tone:"success"|"error"}|null>(null);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const value=new FormData(event.currentTarget).get("status");const response=await fetch(endpoint,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({status:value})});setMessage(response.ok?{text:"Status updated",tone:"success"}:{text:"Unable to update status",tone:"error"});if(response.ok)router.refresh()}return <form onSubmit={submit} className="card mt-8 grid gap-4"><h2 className="text-xl font-bold">Lifecycle</h2><select name="status" defaultValue={status} className="min-h-11 rounded border px-3">{statuses.map(value=><option key={value}>{value}</option>)}</select>{message&&<ToastMessage message={message.text} tone={message.tone}/>}<Button>Update status</Button></form>}
export function UserPolicyForm({id,role,active}:{id:string;role:string;active:boolean}){const router=useRouter();const [message,setMessage]=useState<{text:string;tone:"success"|"error"}|null>(null);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch(`/api/back-office/users/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({role:form.get("role"),active:form.get("active")==="true"})});const body=await response.json();setMessage(response.ok?{text:"User updated",tone:"success"}:{text:body.error??"Unable to update user",tone:"error"});if(response.ok)router.refresh()}return <form onSubmit={submit} className="card mt-8 grid gap-4"><h2 className="text-xl font-bold">Access policy</h2><select name="role" defaultValue={role} className="min-h-11 rounded border px-3"><option>SUPER_ADMIN</option><option>EDITOR</option><option>REVIEWER</option></select><label><input name="active" type="checkbox" value="true" defaultChecked={active}/> Active account</label>{message&&<ToastMessage message={message.text} tone={message.tone}/>}<Button>Save access</Button></form>}
export function DeleteMediaButton({id,disabled}:{id:string;disabled:boolean}){const router=useRouter();const [message,setMessage]=useState<{text:string;tone:"success"|"error"}|null>(null);async function remove(){const response=await fetch(`/api/back-office/media/${id}`,{method:"DELETE"});const body=await response.json();setMessage(response.ok?{text:"Deleted",tone:"success"}:{text:body.error??"Unable to delete",tone:"error"});if(response.ok){router.replace("/back-office/media");router.refresh()}}return <div className="card mt-8"><ConfirmButton label="Delete media" confirmTitle="Delete media asset" confirmBody="This permanently removes the unused media asset from storage and the CMS." confirmLabel="Delete media" className="bg-red-700" disabled={disabled} onConfirm={remove}/>{disabled&&<p className="mt-2 text-sm text-slate-500">Referenced media cannot be deleted.</p>}{message&&<div className="mt-2"><ToastMessage message={message.text} tone={message.tone}/></div>}</div>}
