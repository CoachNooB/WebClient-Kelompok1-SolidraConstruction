"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {Button} from "@/components/ui/button";
export function ResourceStatusForm({endpoint,status,statuses}:{endpoint:string;status:string;statuses:string[]}){const router=useRouter();const [message,setMessage]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const value=new FormData(event.currentTarget).get("status");const response=await fetch(endpoint,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({status:value})});setMessage(response.ok?"Status updated":"Unable to update status");if(response.ok)router.refresh()}return <form onSubmit={submit} className="card mt-8 grid gap-4"><h2 className="text-xl font-bold">Lifecycle</h2><select name="status" defaultValue={status} className="min-h-11 rounded border px-3">{statuses.map(value=><option key={value}>{value}</option>)}</select>{message&&<p role="status">{message}</p>}<Button>Update status</Button></form>}
export function UserPolicyForm({id,role,active}:{id:string;role:string;active:boolean}){const router=useRouter();const [message,setMessage]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch(`/api/back-office/users/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({role:form.get("role"),active:form.get("active")==="true"})});const body=await response.json();setMessage(response.ok?"User updated":body.error??"Unable to update user");if(response.ok)router.refresh()}return <form onSubmit={submit} className="card mt-8 grid gap-4"><h2 className="text-xl font-bold">Access policy</h2><select name="role" defaultValue={role} className="min-h-11 rounded border px-3"><option>SUPER_ADMIN</option><option>EDITOR</option><option>REVIEWER</option></select><label><input name="active" type="checkbox" value="true" defaultChecked={active}/> Active account</label>{message&&<p role="status">{message}</p>}<Button>Save access</Button></form>}
export function DeleteMediaButton({id,disabled}:{id:string;disabled:boolean}){const router=useRouter();const [message,setMessage]=useState("");async function remove(){if(!confirm("Delete this unused media asset?"))return;const response=await fetch(`/api/back-office/media/${id}`,{method:"DELETE"});const body=await response.json();setMessage(response.ok?"Deleted":body.error??"Unable to delete");if(response.ok){router.replace("/back-office/media");router.refresh()}}return <div className="card mt-8"><Button type="button" className="bg-red-700" disabled={disabled} onClick={remove}>Delete media</Button>{disabled&&<p className="mt-2 text-sm text-slate-500">Referenced media cannot be deleted.</p>}{message&&<p role="status" className="mt-2">{message}</p>}</div>}
