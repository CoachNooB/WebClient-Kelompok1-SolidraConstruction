"use client";import {useState} from "react";import {Button} from "@/components/ui/button";import {Input} from "@/components/ui/input";import {ToastMessage} from "@/components/back-office/toast";import type {CompanySettings} from "@/lib/repositories/public-content";
export function SettingsForm({value}:{value:CompanySettings}){const [message,setMessage]=useState<{text:string;tone:"success"|"error"}|null>(null);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const body={name:form.get("name"),email:form.get("email"),phone:form.get("phone"),defaultLocale:form.get("defaultLocale"),descriptionId:form.get("descriptionId"),descriptionEn:form.get("descriptionEn"),socialLinks:{linkedin:form.get("linkedin"),instagram:form.get("instagram"),youtube:form.get("youtube")},defaultSeo:{title:form.get("seoTitle"),description:form.get("seoDescription")}};const response=await fetch("/api/back-office/settings",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify(body)});setMessage(response.ok?{text:"Settings saved",tone:"success"}:{text:"Unable to save settings",tone:"error"})}const social=value.socialLinks??{};const seo=value.defaultSeo??{title:value.name,description:"Solidra Construction corporate website"};return <form onSubmit={submit} className="card mt-8 grid gap-4"><h2 className="text-xl font-bold">Company identity</h2><div className="grid gap-4 md:grid-cols-2"><label>Name<Input name="name" defaultValue={value.name}/></label><label>Email<Input name="email" type="email" defaultValue={value.email}/></label><label>Phone<Input name="phone" defaultValue={value.phone}/></label><label>Default locale<select name="defaultLocale" defaultValue={value.defaultLocale} className="mt-1 min-h-11 w-full rounded border px-3"><option>ID</option><option>EN</option></select></label></div><label>Indonesian description<textarea name="descriptionId" defaultValue={value.descriptionId} className="mt-1 w-full rounded border p-3"/></label><label>English description<textarea name="descriptionEn" defaultValue={value.descriptionEn} className="mt-1 w-full rounded border p-3"/></label><h2 className="text-xl font-bold">Social links</h2><div className="grid gap-4 md:grid-cols-3"><Input name="linkedin" defaultValue={social.linkedin} placeholder="LinkedIn HTTPS URL"/><Input name="instagram" defaultValue={social.instagram} placeholder="Instagram HTTPS URL"/><Input name="youtube" defaultValue={social.youtube} placeholder="YouTube HTTPS URL"/></div><h2 className="text-xl font-bold">Default SEO</h2><Input name="seoTitle" defaultValue={seo.title}/><textarea name="seoDescription" defaultValue={seo.description} className="rounded border p-3"/>{message&&<ToastMessage message={message.text} tone={message.tone}/>}<Button>Save settings</Button></form>}
